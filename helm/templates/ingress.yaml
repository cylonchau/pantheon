{{- if and .Values.server.enabled .Values.server.ingress.enabled }}
{{- if eq .Values.server.ingress.version "v1beta1" }}
  # Ingress resource for v1beta1
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.labels.app }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- toYaml .Values.labels | nindent 4 }}
  annotations:
{{- if and (ge .Values.k8s.version "1.16") (lt .Values.k8s.version "1.18") }}
    kubernetes.io/ingress.class: {{ .Values.server.ingress.class }}
{{- end }}
    nginx.ingress.kubernetes.io/proxy-body-size: 15m
spec:
{{- if ge .Values.k8s.version "1.18" }}
  ingressClassName: {{ .Values.server.ingress.class }}
{{- end }}
  rules:
    - host: {{ .Values.server.ingress.host }}
      http:
        paths:
          - path: {{ .Values.server.ingress.path }}
{{- if not (and (ge .Values.k8s.version "1.16") (le .Values.k8s.version "1.18")) }}
            pathType: Prefix
{{- end }}
            backend:
              serviceName: {{ .Values.labels.app }}-svc
              servicePort: {{ .Values.server.service.port }}  # Service port
  {{- if .Values.server.ingress.tls.enabled }}
  tls:
  - hosts:
      # TLS host
      - {{ .Values.server.ingress.host }}
    # Secret name for TLS
    secretName: {{ .Values.server.ingress.tls.secretName }}
  {{- end }}

{{- else if eq .Values.server.ingress.version "v1" }}
  # Ingress resource for v1
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.labels.app }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- toYaml .Values.labels | nindent 4 }}  # Common labels
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 15m
spec:
  ingressClassName: {{ .Values.server.ingress.class }}
  rules:
    # Host for the ingress
    - host: {{ .Values.server.ingress.host }}
      http:
        paths:
          # Path for the ingress
          - path: {{ .Values.server.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.labels.app }}-svc
                port:
                  # Service port
                  number: {{ .Values.server.service.port }}
{{- if .Values.server.ingress.tls.enabled }}
tls:
  - hosts:
      # TLS host
      - {{ .Values.server.ingress.host }}
      # Secret name for TLS
    secretName: {{ .Values.server.ingress.tls.secretName }}
{{- end }}
{{- end }}
{{- end }}
